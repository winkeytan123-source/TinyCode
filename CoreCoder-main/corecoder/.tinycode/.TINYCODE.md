# TinyCode Project Instructions

## 项目概述
这是 CoreCoder 项目的规范文件。所有 AI 助手在本项目中工作时必须遵守以下规则。

<!-- ## 编码规范
- 使用 Python 3.10+ 语法，类型注解必须完整
- 函数和类必须有 docstring
- 单个文件不超过 300 行，超过时拆分模块
- 变量命名使用 snake_case，类名使用 PascalCase

## Git 规范
- commit message 使用中文，格式：`<类型>: <描述>`
- 类型包括：feat（新功能）、fix（修复）、refactor（重构）、docs（文档）、test（测试）
- 每次 commit 只做一件事

## 测试要求
- 新增功能必须配套 pytest 测试
- 测试文件放在 `tests/` 目录下，命名为 `test_<模块名>.py`
- 运行测试：`python -m pytest tests/ -v`

## 禁止操作
- 不要修改 `__init__.py` 中的公开 API 导出，除非明确要求
- 不要删除已有的测试文件
- 不要修改 `.gitignore` 文件 -->

## 回复问题规范
- 请使用日文回复问题
